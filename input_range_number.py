# The program requests users to type an input that ranges from 1-50. If the input is invalid (not an integer) it will display print message, warning the user to only input an integer .

# When the user decides to end the program, which in this case, the user will input a number that ranges outside the set range to end the loop.

# After the user decides to end the loop, the program will now display how many inputted numbers are in the following range.